# 图片策略

## 普通公众号文章

- 正文至少 4 张图。
- 第一张图作为公众号封面，必须优先来自 `images/persona/scenes/` 的同一人设图池，并通过 `image_pool.txt` 的 `COVER_*` 段引用。
- `images/cover/` 里的旧封面只作为历史重发或临时兜底，不作为新文章主封面来源。
- 第二张图优先来自 `drama_image_pool.txt`，使用年轻、现代、彩色的影视/生活剧关系图、男女主合照或生活化关系截图。
- 后 2 张正文氛围图来自 `image_pool.txt` 的 `BODY` 段。
- 本地图不强制裁剪为 `900x600`，但必须是横图，至少 `900x600`，宽高比建议 `1.25-1.95`，推荐 `3:2` 或接近公众号封面比例。
- 同批封面不重复，并优先选择最近 12 篇里使用次数最少的封面。
- 影视剧照避开最近 12 篇已用图，尽量避免同一来源场景重复。

## 视觉调性

- 普通文章封面固定使用同一位成年女性形象，建立账号识别。
- 封面主风格改为青春靓丽、清爽明亮、元气自然的成年女性生活照。
- 可以有白T、浅色衬衫、牛仔外套、运动短袖、卫衣、清爽裙装和自然姿态。
- 允许轻微露肤，但不得出现露点、裸露敏感部位、透明衣物、内衣特写或低俗挑逗姿势。
- 图片需要清晰、明亮、有色彩吸引力，不使用偏暗、偏糊、尺寸不统一的图片。

## 同一人设生成路线

### 通义万相主体库（当前主路线）

适合快速建立“同一个女人”的长期封面图池。目标是稳定五官、人脸一致，同时保留 iPhone 原相机朋友圈随拍质感。

1. 生成基准形象：
   - 文生图生成 1 张干净正面亚洲成年女性高清照。
   - 要求柔光棚拍、无遮挡五官、自然皮肤纹理、不过度磨皮。
   - 保存为人设底图，建议路径：`images/persona/master/persona_master_wanxiang_source.png`。
2. 上传主体库：
   - 上传基准图到通义万相「主体库」。
   - 命名为虚拟女生角色，不使用真实人物姓名。
   - 后续封面和写真统一调用该主体，锁定五官一致性。
3. 生成场景图：
   - 场景提示词写生活化动作和环境，主打青春靓丽、清爽自然、朋友圈随拍，不写色情姿态或未成年感。
   - 优先使用“iPhone 原相机实拍、自然光、朋友圈随拍、真实皮肤纹理、背景轻微虚化”等描述。
4. 入池处理：
   - 导出后保留高分辨率横图，不强制裁剪到 `900x600`。
   - 最低尺寸 `900x600`，推荐 `1536x1024`、`1280x853`、`1200x800` 或 `900x600`。
   - 如果人物脸部靠边或被平台裁切风险高，再人工裁成安全横图。
   - 放入 `images/persona/scenes/` 或 `images/cover/`。
   - 确认像同一人、不过度写真、不像 AI 棚拍后，再加入 `image_pool.txt`。

可用提示词示例：

```text
iPhone14 原相机实拍，22 岁亚洲成年女性，齐肩黑发，白色T恤搭配浅色牛仔外套，在明亮咖啡店窗边吃巴斯克蛋糕，自然光虚化背景，露齿笑，青春活力，阳光明亮，皮肤真实纹理，高清照片
```

```text
iPhone14 原相机实拍，22 岁亚洲成年女性，户外徒步，宽松浅色运动短袖搭配运动长裤，白天山路，挥手大笑，元气满满，真实皮肤纹理，高清照片
```

详细操作见：

```text
images/persona/wanxiang_playbook.md
images/persona/wanxiang_scene_backlog.md
```

## 贴图版

- 输出到 `articles/image_posts/`，不覆盖原始文章。
- 默认自动重写一个 20 字以内的新标题。
- 使用独立青春活力成年女性图片作为封面。
- 正文排成纯文字卡片，不复用原文章封面或正文剧照作为文字卡片背景。
