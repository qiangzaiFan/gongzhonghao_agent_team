# 目录说明

更新时间：2026-07-30

这个仓库是一个多赛道公众号内容生产工作区。不同目录对应不同垂直账号或内容赛道，主要用于生成本地 Markdown 草稿、做质量检查、管理配图素材，部分目录支持把文章推送到微信公众号草稿箱。

默认可以把它理解成：

```text
gongzhonghao_agent_team/
├── astrology_content/      星座短文内容生产
├── elder_healing_agent/    养老情感疗愈内容生产
├── emotion_women/          情感女性内容生产
├── food_home_cooking/      家常美食内容生产
├── investment_insights/    投资财经内容生产与图表脚本
├── media_agent/            AI 科技资讯内容生产
├── military_frontier/      军事前沿内容生产
├── .agents/                Agent 相关预留目录，当前为空
├── .venv/                  Python 虚拟环境
└── tmp/                    临时文件与图片生成缓存
```

## 快速速查

| 目录/文件 | 用途 | 平时怎么看 |
| --- | --- | --- |
| `.agents/` | Agent 配置或状态的预留目录。当前目录为空。 | 暂时不用管。 |
| `.venv/` | Python 虚拟环境，放依赖包和解释器脚本。 | 不手动改里面的文件；运行脚本时可用它的 Python。 |
| `astrology_content/` | “安夏星座短文号”工作区，每日星座短文、本地草稿、排期、质检。 | 看 `README.md`、`articles/`、`specs/`。 |
| `elder_healing_agent/` | “养老疗愈”公众号 Agent，面向中老年人的晚年自渡、情绪养生、子女边界、关系断舍离内容。 | 看 `CLAUDE.md`、`.claude/agents/elder-healing-writer.md`、`specs/`。 |
| `emotion_women/` | 情感、女性成长、生活日记类公众号 Agent。支持生成、链接改写、AIGC 检测、配图池、草稿箱发布。 | 看 `README.md`、`使用说明.md`、`articles/`、`images/`。 |
| `food_home_cooking/` | 家常美食公众号写作系统，基于“暖暖小厨”风格抽象生成原创草稿。 | 看 `README.md`、`specs/`、`articles/`。 |
| `investment_insights/` | 投资资讯、股票市场、财经分析类自动化脚本；还包含多只股票/市场主题的图表生成脚本。 | 主要入口是 `daily_investment_news.py`，图表脚本按文件名找。 |
| `media_agent/` | AI 科技资讯公众号自动生成系统，主编-写手协作，搜索近 24 小时热点并生成草稿。 | 看 `README.md`、`使用说明.md`、`daily_ai_news.py`。 |
| `military_frontier/` | 军事前沿自动化内容脚本，根据近 24 小时军事新闻生成分析文章。 | 主要入口是 `daily_military_frontier.py`。 |
| `tmp/` | 临时目录，目前主要存 `imagegen` 生成的图片缓存。 | 可查看临时结果；清理前确认没有要保留的图。 |
| `.gitignore` | Git 忽略规则。 | 控制哪些缓存、日志、凭证不提交。 |
| `requirements-imagegen.txt` | 图片生成环境依赖。 | 配置 imagegen 能力时会用到。 |

## 各目录说明

### `.agents/`

当前为空。它更像是为 Agent 配置、运行状态或后续扩展留下的位置。现在没有业务文件，日常可以忽略。

### `.venv/`

本项目的 Python 虚拟环境目录，里面有独立的 Python、pip 和已安装依赖。它不是业务代码目录。

常见用途：

- 用 `.venv/Scripts/python.exe` 运行项目脚本。
- 用 `.venv/Scripts/pip.exe` 安装或查看依赖。
- 不建议手动编辑、移动或删除里面的文件。

### `astrology_content/`

星座短文号工作区，README 中定义为“安夏星座短文号”。主要目标是生成每日 3 篇短平快星座文，本地保存为 Markdown 草稿，并通过质量门槛检查原创度、标题复用和 AIGC 风险。

关键内容：

- `README.md`：完整使用说明。
- `CLAUDE.md`：主编职责和操作约定。
- `specs/`：账号定位、文章质量、来源策略、工作流、安夏风格等规则。
- `articles/`：已生成的星座短文草稿。
- `anxia_generate.py`：批量生成短文。
- `anxia_calendar.py`：生成选题排期。
- `quality_gate.py` / `preflight.py`：文章质检与发布前检查。
- `ai_detector.py`：中文 AIGC 检测能力，被其他目录复用。

适合在这里做：

- 批量生成星座短文。
- 检查星座稿是否重复、是否像 AI、是否符合账号风格。
- 维护星座号的风格规范和选题排期。

### `elder_healing_agent/`

养老情感疗愈公众号工作区，用于生产面向 50 岁以后读者的晚年自渡、情绪养生、子女边界、钱与底气、关系断舍离和病后重启类文章。

关键内容：

- `CLAUDE.md`：主编配置、语料范围、成稿约定和工作流。
- `.claude/agents/elder-title-editor.md`：养老疗愈标题官和选题编辑。
- `.claude/agents/elder-healing-writer.md`：养老疗愈顶级写手 Agent。
- `.claude/agents/elder-chief-editor.md`：主编二审，负责原创度、风险和机器稿味。
- `specs/`：账号定位、写手手册、标题系统、原创插图规范、30 天游题表。
- `templates/`：选题卡和文章模板。
- `daily_elder_healing.py`：从 30 天起号表生成写手任务。
- `pipeline.py`：生成完整生产包，包含标题官、写手、二审和质检任务。
- `quality_gate.py`：100 分制质检，检查标题、篇幅、风险、参考语料相似度和历史稿去重。
- `ai_detector.py`：养老疗愈专用本地中文 AIGC 检测封装，报告保存到 `reviews/auto/`。
- `scripts/plan_article_illustrations.py`：为每篇文章规划 3 张原创漫画插图，生成 prompt、插入图位并写入去重清单。
- `scripts/compose_quote_cards.py`：读取无字水彩底图，合成准确中文大字、自有红章和最终金句卡 PNG。
- `scripts/analyze_reference_corpus.py`：分析本地参考语料并输出统计报告。
- `scripts/batch_quality.py`：批量质检 `articles/` 并输出复盘摘要。
- `articles/`：生成的本地 Markdown 草稿。
- `images/illustrations/`：养老疗愈文章原创插图与出图 prompt。

适合在这里做：

- 为养老疗愈公众号起号。
- 生成“身体重启、情绪养生、子女边界、钱与底气、独处自渡”等短文。
- 把参考博主的高层选题和情绪节奏转化为原创稿。

注意：

- 只学习参考博主的高层写法，不复用原文标题、段落、署名、案例和图片。
- 插图也只学习高层视觉方法，不搬运悦漫先生原图、印章、署名、图中文字或可识别构图。
- 医疗、财务、亲子冲突类内容需要人工复核，不做诊断、疗效承诺或投资建议。

### `emotion_women/`

情感女性公众号工作区，用于生成情感、女性成长、婚恋、家庭、职场压力、生活日记类文章。这个目录功能最完整，既有写作 Agent，也有链接改写、配图、人设图、质检、发布草稿箱等链路。

关键内容：

- `README.md` / `使用说明.md`：完整说明。
- `daily_emotion_women.py`：生成情感女性文章。
- `rewrite_from_link.py`：从爆款链接做原创转化稿。
- `rewrite_direct_from_link.py`：保留来源标题，只改写正文。
- `publish_existing_article.py`：重试发布已有文章到公众号草稿箱。
- `quality_gate.py`：标题、结构、图片、内容比例等质检。
- `ai_detector.py`：本目录内的 AIGC 检测入口。
- `articles/`：生成的文章草稿。
- `images/`：封面、正文图、人物图、剧照等素材。
- `specs/`：账号定位、平台规则、图片策略、文章质量、工作流。
- `logs/`：运行日志和发布记录。
- `reviews/auto/`：自动质检或 AIGC 检测报告。

适合在这里做：

- 生成情感/女性成长文章。
- 把外部链接改造成原创选题草稿。
- 管理固定人物封面图池和正文配图。
- 发布到微信公众号草稿箱，但不会直接群发，仍需人工审核。

注意：

- 涉及 `.mcp.json`、公众号 AppID/AppSecret、API Key 的配置不要提交到 Git。
- `--publish` 只推送到公众号草稿箱，不等于正式群发。

### `food_home_cooking/`

家常美食公众号工作区，基于本地“暖暖小厨”知识库抽象出来的写作系统，用于生产原创家常饭公众号草稿。

关键内容：

- `README.md`：使用说明。
- `CLAUDE.md`：主编入口。
- `quality_gate.py`：本地质检。
- `specs/nuannuan_style.md`：暖暖小厨风格拆解。
- `specs/persona.md`：人设与地域。
- `specs/food_track_plan.md`：运营计划。
- `specs/title_and_dedup_rules.md`：标题和去重规则。
- `articles/`：美食文章草稿。
- `reviews/auto/`：自动检测报告。

适合在这里做：

- 写“晒饭”“家常菜”“预算餐”“下班快手饭”这类文章。
- 按选题卡生成原创 Markdown 草稿。
- 检查标题、图片数量、花费明细、小失误、互动语等是否达标。

注意：

- 这个目录强调原创边界：只学习高层写法，不复用知识库原文标题、段落、图片、署名或可识别表达。
- 发布前需要补齐自有实拍图片。

### `investment_insights/`

投资财经内容目录。当前没有 README，本说明根据脚本名和 `daily_investment_news.py` 开头注释整理。

主要用途是自动生成投资资讯、股票市场、投资策略、财经分析、财富管理相关内容。目录里还放了多个图表生成脚本，用于特定公司或市场主题的配图/数据图。

关键内容：

- `daily_investment_news.py`：投资资讯自动化生成与发布脚本，支持立即执行、定时执行、自定义文章数量。
- `generate_market_chart.py`：市场图表生成。
- `generate_btc_charts.py` / `generate_btc_crash_charts.py`：比特币主题图表。
- `generate_aws_charts.py`：AWS 主题图表。
- `generate_xpeng_charts.py`：小鹏汽车主题图表。
- `create_apple_charts.py`、`create_jd_charts.py`、`create_netflix_charts.py` 等：单公司或单事件图表。
- `.claude/agents/`：投资写作相关 Agent 定义目录。

适合在这里做：

- 生成近 24 小时投资/财经热点文章。
- 围绕财报、市场、宏观、科技股、中概股等方向做资讯稿。
- 为文章生成财经图表。

注意：

- 财经内容属于高风险信息，正式发布前要人工核对数据来源、时间、口径和风险提示。
- 如果脚本启用发布功能，先确认 `.mcp.json`、公众号凭证和发布目标无误。

### `media_agent/`

AI 科技资讯公众号自动生成系统。README 中说明它采用“主编-写手”协作模式：Python 脚本负责调度，Claude Code 主进程做主编，写手 Agent 做研究和成稿。

关键内容：

- `README.md`：项目概览。
- `使用说明.md`：详细运行说明。
- `daily_ai_news.py`：核心调度脚本。
- `CLAUDE.md`：项目级主编配置。
- `.claude/agents/ai-news-writer.md`：AI 新闻写手 Agent。
- `output/`：生成的 Markdown 草稿和发布记录，通常被 Git 忽略。

适合在这里做：

- 搜索最近 24 小时 AI/科技热点。
- 生成高信息密度科技资讯稿。
- 通过 `--publish` 推送到微信公众号草稿箱。

常见入口：

```bash
cd media_agent
python daily_ai_news.py --now --count 1 --verbose
```

### `military_frontier/`

军事前沿内容目录。当前没有 README，本说明根据 `daily_military_frontier.py` 整理。

主要用途是根据近 24 小时军事新闻生成军事分析文章，关注高超音速导弹、无人作战、军事 AI、俄乌战争、南海、台海、军演、装备现代化等主题。

关键内容：

- `daily_military_frontier.py`：军事前沿自动化生成与发布脚本。
- `.claude/agents/`：军事内容相关 Agent 定义目录。
- 脚本运行时可能创建 `articles/`、`images/`、`logs/`、`prompt.md` 等输出。

适合在这里做：

- 生成军事技术、地缘军事态势、实战案例、军事演习分析文章。
- 搜索近 24 小时军事新闻并筛选不重复选题。

注意：

- 脚本提示词里包含发布到公众号草稿箱、发布图片消息等动作。运行前要确认发布配置、素材版权、事实来源和账号目标。
- 军事内容要特别注意事实准确性、来源可靠性和平台合规。

### `tmp/`

临时目录。当前主要是 `tmp/imagegen/`，里面存放图片生成过程中的临时图片，例如人物候选图、运动主题图等。

适合在这里做：

- 临时查看 imagegen 生成结果。
- 挑选后把正式可用图片移动到对应业务目录，比如 `emotion_women/images/`。

注意：

- `tmp/` 不是正式资产库。
- 清理前先确认里面没有要保留的成图。

## 其他关键文件

### `.gitignore`

Git 忽略规则。通常会忽略虚拟环境、输出、日志、临时文件、凭证配置等，避免把机器环境和敏感配置提交进仓库。

### `requirements-imagegen.txt`

图片生成环境依赖文件。需要配置或恢复 imagegen 能力时，用它安装相关依赖。

### `.git/`

Git 版本库内部目录，普通使用不需要打开或修改。不要手动删除、移动或编辑里面的文件。

## 使用建议

- 想看某个赛道怎么用：先打开对应目录的 `README.md` 或 `使用说明.md`。
- 想找生成出来的稿子：优先看各目录的 `articles/` 或 `output/`。
- 想找规则和风格：优先看 `specs/`、`CLAUDE.md`、`.claude/agents/`。
- 想跑质检：找对应目录的 `quality_gate.py`、`preflight.py` 或 `ai_detector.py`。
- 带 `--publish` 的命令会触发公众号草稿箱相关流程，运行前先确认凭证、账号和发布目标。
- `.venv/`、`tmp/`、`logs/`、`output/`、`.mcp.json` 一般不作为正式内容提交。
