# 项目协作规则

这个仓库是多赛道微信公众号内容生产工作区。默认按低 token、轻量协作方式处理任务。

## 默认工作方式

- 先做最小必要读取、最小必要命令和简短回复。
- 用户说“低token模式”“低 token 模式”“只执行命令”“少解释”时，不做宽泛文档检索，不扫描无关目录，不输出长计划。
- 纯操作类请求直接执行，例如：`git status`、拉取远程、提交/推送当前改动、查文件位置、列目录、查看最近提交、回答已有本地状态。
- 简单问答或目录查询只读取直接相关文件；除非用户明确要求“查官方文档/联网确认/完整审计”，否则不联网、不做大范围仓库搜索。
- 小范围文档、配置或脚本改动可以直接修改，并用 `rg`、`git diff --check`、相关轻量测试验证。

## 赛道边界

- 改某个赛道前，优先读该赛道的 `README.md`、`CLAUDE.md` 和 `specs/` 中直接相关文件。
- 除非用户明确要求跨赛道修改，否则变更只限于用户指定赛道。
- 不做无关重构，不移动无关文件，不重写已有内容风格。

赛道速查：

- `astrology_content/`：星座短文、十二星座日运卡、质量门槛和表现复盘。
- `emotion_women/`：情感女性文章、链接改写、图池、AIGC 检测和草稿箱发布。
- `food_home_cooking/`：家常美食文章、本地配图流水线和原创边界。
- `elder_healing_agent/`：养老情感疗愈文章、原创水彩金句卡和发布质检。
- `media_agent/`：AI 科技新闻生成和草稿箱发布。
- `investment_insights/`：财经内容与图表；事实和风险提示需要人工复核。
- `military_frontier/`：军事分析内容；事实、来源和合规需要人工复核。

## 安全边界

- 不提交凭证、`.mcp.json`、API key、虚拟环境、日志、生成缓存或临时图片。
- `--publish`、微信公众号草稿箱上传、凭证读取和外部平台写入都必须先经用户确认。
- 遇到未提交改动时，先确认来源和范围；不要回滚用户改动。

## 验证与提交

- 改代码后运行最贴近改动的轻量测试或语法检查。
- 提交前运行 `git status -sb` 和 `git diff --check`。
- 推送前确认当前分支和远程状态，优先避免强推。
